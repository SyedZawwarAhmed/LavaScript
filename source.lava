@ if (a>3) {
  until(fun()) {
    if (n*n == fun()) {
    
    }
  }
} @


interface B {
    method GetArea(c: string): number;
}

interface E {
    method GetArea(): number;
} 

dynamic c: number;

proc abc(a: string, a: string): string {
  dynamic c: number = 5 + 8;
}

class C {}
class D {}